---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import { teaching } from "../../../content/teaching-sk.js";
---

<BaseLayout title="Výuka — ratrak.sk" lang="sk">
  <main class="container">
    <header class="hero">
      <h1>Výuka</h1>
      <p class="lead">
        Malé kurzy a workshopy: micro:bit, Python a učenie sa hrou.
      </p>

      <div class="card">
        <strong>Kontakt:</strong>
        <span class="muted">Najrýchlejší spôsob je LinkedIn DM.</span>
      </div>

      <div class="meta" style="margin-top: 10px;">
        <a
          href="https://www.linkedin.com/in/lucia-parcová-šálková-bb935564/"
          target="_blank"
          rel="noreferrer"
        >
          otvoriť LinkedIn
        </a>
        <a href="/sk">domov</a>
      </div>
    </header>

    <div class="stack">
      <section class="section">
        <h2>Čo vyučujem</h2>

        <div class="grid">
          {teaching.map((t) => (
            <article class="item">
              <div class="item-title">
                <strong>{t.title}</strong>
                <span class="pill">{t.tag}</span>
              </div>

              <p>{t.description}</p>

              {t.meta && (
                <div class="meta">
                  {t.meta.map((m) => (
                    <span>{m}</span>
                  ))}
                </div>
              )}
            </article>
          ))}
        </div>
      </section>

      <section class="section">
        <h2>Ako vyučujem</h2>
        <div class="grid">
          <article class="item">
            <p>Zameriavam sa na pochopenie a sebavedomie, nie na rýchle prejdenie syntaxe.</p>
            <p>Budujeme malé projekty, spoločne ladíme a učíme sa klásť dobré otázky.</p>
          </article>
        </div>
      </section>
    </div>
  </main>
</BaseLayout>
